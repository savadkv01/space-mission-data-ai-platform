# serving/requirements.txt — offline dev + serving stack.
# The pure-Python marts + tests need only pytest; everything else activates when
# the serving/BI/API infrastructure is provisioned.

# --- offline (marts + tests) ---
pytest>=8.0

# --- serving engine + models (activate with infra) ---
# dbt-duckdb>=1.7        # serving dbt models on DuckDB
# duckdb>=0.10           # serving SQL DDL / direct queries

# --- API (stubbed for Phase 16) ---
# fastapi>=0.110
# uvicorn>=0.29
# redis>=5.0             # API response cache
