# ============================================================================
# Ingestion layer — Python dependencies (open-source only)
# ============================================================================
requests==2.32.3
python-dotenv==1.0.1
kafka-python==2.0.2          # Kafka producers/consumers
boto3==1.34.131             # MinIO (S3-compatible) landing zone
sgp4==2.23                  # TLE orbit propagation (optional; analytic fallback)

# --- Dev / test ---
pytest==8.2.2
